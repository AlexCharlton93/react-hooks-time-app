/* eslint-disable no-undef */
describe("MetricsManager", () => {
    describe("getMetrics", () => {
        it("Should return the correct JSON response", async () => {
            done();
        });
    });
});
